<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.openclassrooms.mynews">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controllers.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".controllers.activities.DetailActivity"
            android:parentActivityName=".controllers.activities.MainActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.openclassrooms.mynews.controllers.activities.MainActivity" />
        </activity>

        <activity android:name=".controllers.activities.SearchActivity"
            android:parentActivityName=".controllers.activities.MainActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.openclassrooms.mynews.controllers.activities.MainActivity" />
        </activity>

        <activity android:name=".controllers.activities.NotificationActivity"
            android:parentActivityName=".controllers.activities.MainActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.openclassrooms.mynews.controllers.activities.MainActivity" />
        </activity>

        <activity android:name=".controllers.activities.DisplaySearchActivity"
            android:parentActivityName=".controllers.activities.MainActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.openclassrooms.mynews.controllers.activities.MainActivity" />
            </activity>

    </application>

</manifest>